#include scripts\utility\challenge_utility;

// Perks and upgrades here ? 
// Additional logic
// Involve map logic/structure ? 
// Database structure, save every prestige, and challenges for that prestige that were completed ...
// Special round activations

challengeRoundNinja()
{
	self iprintln("Ninja challenge round not ready yet");
}

challengeRoundDemolition()
{
	self iprintln("Demolition challenge round not ready yet");
}

challengeRoundHitman()
{
	self iprintln("Hitman challenge round not ready yet");
}

challengeRoundJuggernaut()
{
	self iprintln("Juggernaut challenge round not ready yet");
}

challengeRoundDaredevil()
{
	self iprintln("Daredevil challenge round not ready yet");
}

challengeRoundWeirdo()
{
	self iprintln("Weirdo challenge round not ready yet");
}

challengeRoundAssault()
{
	self iprintln("Assault challenge round not ready yet");
}

challengeRoundTactician()
{
	self iprintln("Tactician challenge round not ready yet");
}

challengeRoundTrickster()
{
	self iprintln("Trickster challenge round not ready yet");
}


// Stat trackers are currently located in _missions.gsc

// Challenge round objectives here or in the same functions above ?
// Challenge -> New thread -> Blocked untill each step is passed till the final completion ?
// Checkers for common stuff ?  